import GradientBar from "./GradientBar";

type Props = {};

const HeaderNavMenu = (props: Props) => {
	return (
		<div
			style={{minHeight: 45, maxHeight: 45, display: "flex", width: '100%', backgroundColor: '#37373c'}}
		>
		</div>
	);
};
export default HeaderNavMenu;
